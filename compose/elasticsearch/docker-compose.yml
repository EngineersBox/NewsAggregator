version: "3.5"
services:
    elasticsearch:
        container_name: elasticsearch
        build:
            context: .
        environment:
            discovery.type: single-node
            bootstrap.memory_lock: "true"
        ulimits:
            memlock:
                soft: -1
                hard: -1
        ports:
            - 9200:9200